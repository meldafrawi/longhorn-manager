FROM ubuntu:16.04

RUN apt-get update && apt-get install -y curl vim nfs-common iproute dnsutils iputils-ping telnet zip

ADD /drone/src/launch-manager /usr/local/sbin/
ADD /drone/src/bin /usr/local/sbin/
ADD /drone/src/driver /

VOLUME /usr/local/sbin
EXPOSE 9500
CMD ["launch-manager"]
